### Python Websocket
#### Description
Websocket written in pure python running on an ASGI Uvicorn server.

#### Installation
```
pip install pipenv
pipenv sync
pipenv shell
```

### Start
```
uvicorn app:app
```